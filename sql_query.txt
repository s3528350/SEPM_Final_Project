DROP DATABASE if exists humanoid;
create database humanoid;
use humanoid;
CREATE TABLE IF NOT EXISTS users (id INT(12) UNSIGNED AUTO_INCREMENT PRIMARY KEY, fname VARCHAR(50) NOT NULL, lname VARCHAR(50) NOT NULL,email VARCHAR(50) NOT NULL, password CHAR(40), confirmPassword CHAR(40), is_admin boolean, created_at VARCHAR(40));
insert into users values(null, 'admin', 'user', 'admin@humanoid.com',SHA('1234'),SHA('1234'),true,now() );